services:
  backend:
    build:
      context: ./packages/backend
      dockerfile: Dockerfile
    env_file:
      - ./packages/backend/.env
    environment:
      PORT: 10000
    ports:
      - "8080:10000"
